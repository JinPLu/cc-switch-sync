# Never commit the CC Switch database — it contains plaintext API keys
*.db
*.db-shm
*.db-wal

# Local server registry — contains real IPs/hostnames and proxy addresses
servers.conf
servers.json

# Local state dirs (history downloads, user config)
.cc-switch-sync/
.cc-remote/
.claude/

# Release artifacts
release/
*.zip

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp
*~

# Private AI assistant notes — not for upstream
CLAUDE.md
AGENTS.md
